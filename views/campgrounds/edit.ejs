<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partial/BootstrapStyle.ejs')  %> 
    <title>Edit Campground</title>
</head>
<body>
    <h1>Edit campground : <%= c.title %></h1>
    <form action="/campgrounds/<%= c._id %>?_method=PATCH"" method="POST">
        <div>
            <label for="title">camp name : </label>
            <input type="text" id="title" name="campground[title]" placeholder="<%= c.title %>" value="<%= c.title %>">
        </div>
        <div>
            <label for="location">location : </label>
            <input type="text" name="campground[location]" id="location" placeholder="<%= c.location %>" value="<%= c.location %>">
        </div>
        <button>Update Campground</button>
    </form>
    <div><a href="/campgrounds/<%= c._id %>">Back to <%= c.title %></a></div>
    <div><a href="/campgrounds">back to all campgrounds</a></div>
</body>
</html>